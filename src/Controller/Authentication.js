import {db} from '../Database/DB.js';

async function przyklad(req, res){
    // const userData = await db.users.getUserData();
    // res.status(200).send({data : userData})
}

export const Authentication = {
    przyklad: przyklad,
}